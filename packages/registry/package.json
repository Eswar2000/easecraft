{
  "name": "easecraft-registry",
  "version": "0.0.0",
  "private": true,
  "description": "Typed metadata and copyable component source for Easecraft.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "node -e \"console.log('easecraft-registry: build placeholder')\"",
    "lint": "node -e \"console.log('easecraft-registry: lint placeholder')\"",
    "test": "node -e \"console.log('easecraft-registry: test placeholder')\"",
    "typecheck": "tsc --noEmit --project tsconfig.json"
  },
  "devDependencies": {
    "easecraft-typescript-config": "workspace:*"
  }
}