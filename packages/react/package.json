{
  "name": "easecraft",
  "version": "0.0.0",
  "private": true,
  "description": "Accessible React motion primitives, hooks, and components.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "node -e \"console.log('easecraft: build placeholder')\"",
    "lint": "eslint . --max-warnings 0",
    "test": "node -e \"console.log('easecraft: test placeholder')\"",
    "typecheck": "tsc --noEmit --project tsconfig.json"
  },
  "devDependencies": {
    "easecraft-eslint-config": "workspace:*",
    "easecraft-typescript-config": "workspace:*"
  }
}
