{
  "name": "easecraft-tokens",
  "version": "0.0.0",
  "private": true,
  "description": "Framework-independent motion tokens for Easecraft.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "node -e \"console.log('easecraft-tokens: build placeholder')\"",
    "lint": "eslint . --max-warnings 0",
    "test": "node -e \"console.log('easecraft-tokens: test placeholder')\"",
    "typecheck": "tsc --noEmit --project tsconfig.json"
  },
  "devDependencies": {
    "easecraft-eslint-config": "workspace:*",
    "easecraft-typescript-config": "workspace:*"
  }
}
